# Daily standup — 2026-08-19

Generated at 07:02 America/Montreal after a complete 32-task monitoring and
action cycle.

## Board pulse

- 3 platform implementations are actively running in Work.
- 15 tasks are waiting in Human-QA.
- 8 tasks are in Work: 3 active, 5 intentionally blocked or parked.
- 1 Review and 1 CI-Fixup task are anomalously held by stale RUNNING sessions.
- 6 tasks are in ToDeploy awaiting deployment/integration cleanup.
- No unresolved ask-channel question or architectural decision is waiting on the
  human.

## Completed or materially progressed since the previous standup

- Managed repository credentials: canonical PR #2787 landed. GitHub SSH/plugin
  repository records now resolve through the shared HTTPS identity policy, with
  launch preflight and legacy provider-host backfill coverage.
- Workflow reset handling: PR #2765 merged after the resume-token generation and
  reset-ordering race was corrected and independently verified.
- Coordinator host contract: `9e67c426` reached Human-QA on canonical host PR
  #2793. Runtime dispatch, durable occurrence claims, profile validation,
  WorkspaceAgentChat, config schema, and per-workflow monitoring persistence are
  implemented and tested.
- Post-merge E2E runner repair: `55d2d589` / PR #2800 reached Human-QA on head
  `6d3129dc` after fresh Review and distinct QA passed 21 focused tests, shell
  syntax, ESLint, typecheck, and direct runner-environment probes.
- Tags self-service tool: `21e3b8b8` produced canonical plugin PR #6 with
  add/remove/list task tools, trusted context, concurrency-safe storage,
  truncation/projection coverage, package verification, and green CI. Its final
  workflow gate is currently affected by the stale-session platform defect.
- Provider Usage: the host and real packaged plugin passed all four gated E2Es
  (kanban and office discovery/call, schema rejection, External absence).
  Missing plugin concurrency/schema/shared-cache coverage was isolated and
  preserved at recovered commit `a5c386b`.
- Workspace reuse: `957da1cb` committed attach-only reuse, the durable behavior
  contract, atomic canonical workspace/session binding, and inherited-workspace
  fail-closed handling (`b67cd5b82`, `bb57137c3`, `f252aeb65`, `a55302075`).
- Git metadata permissions: `b74833e7` committed the shared resolver/admission
  slice (`62c369b30`) and is now committing narrow Docker layered mounts that
  replace the broad writable GitDir bind.
- Container user namespaces: `6a5a2f73` implemented the seccomp/AppArmor profile,
  operator-only config, frontend controls, handler coverage, and real enabled/
  disabled bwrap E2E. The test is ready but still depends on the host network
  repair.

## Coordinator actions and decisions

- Kept workflow columns authoritative and stopped using Backlogs as a provider
  switching mechanism. Work/Review on-entry automation owns normal Claude
  launches.
- Established the fallback order Claude → GPT-5.3-Codex-Spark → other Codex →
  Copilot DI. Spark receives only a clean, bounded, implementation-ready brief
  and must restate the spec before risky edits.
- Split two adjacent platform defects:
  - `957da1cb`: canonical task workspace reuse and duplicate materialization.
  - `b74833e7`: narrowly writable linked-worktree Git administrative metadata.
- Created `c642d57a` for durable delivery/turn settlement and guarded stale-session
  recovery. Its complete design advanced Todo → Work and implementation is
  running.
- Corrected premature acceptance routes: Provider Usage returned to Work for
  missing mandatory coverage; PR #2800 received fresh Review and QA after its
  head changed.
- Replaced the unusable Provider Usage recovery attempt with task `212a68ce` and
  recorded a formal dependency on `b74833e7` after a fresh checkout reproduced
  the read-only `index.lock` failure.

## Active work

- `c642d57a` — implementing durable completion intent/settlement, transition-keyed
  on-entry delivery, bounded retry, and authorized stale-session recovery.
  Initial RED repository test is running with the supplied mise Go toolchain.
- `957da1cb` — finishing shared-group election, workspace-mode entry points, and
  race semantics after commit `a55302075`; focused suites and hooks are green.
- `b74833e7` — finishing Docker/standalone permission enforcement, atomic
  later-source refresh, Git locking/isolation, and real restore/add/commit
  acceptance. Provider Usage remains blocked until this matrix passes.

## Awaiting human testing

- `9e67c426` — coordinator host contract, PR #2793, preserved head `e23a49f8`.
- `725d47ae` — hermetic `internal/github` tests, PR #2792.
- `55d2d589` — post-merge E2E runner fixes, PR #2800, head `6d3129dc`.
- `9ebf7cdc` — terminal-session reuse fix, PR #2766.
- `7ca86e53` — Redmine integration, PRs #2706/#2724.
- `96e27238` — plugin-hook docs/template cleanup, plugin-template PR #4.
- `7dac85e2` — per-MR GitLab automation controls, PR #2676.
- `45f95870` — plugin update settings, PR #2513.
- `ae8fc022` — tag display/sidebar integration, host PR #2609.
- `d30ca136` — rich GitLab/task hover previews, PR #2610.
- `dabb2da9` — file-tree rename focus stabilization, covered by PR #2610.
- `51781b28` — plugin webhook authorization, PR #2608.
- `e9f9198c` — GitLab environment-test guard, PR #2693.
- `e9c15a27` — mobile workspace-source Add-folder fix, PR #2696.
- `9ededcef` — executor-field preservation, PR #2705.

## Needs human action

- Finish the in-progress Kandev instance/network update and signal the
  Coordinator. That signal unblocks the signed Debian build probes in
  `dd4f90b0` and the exact enabled/disabled userns+bwrap E2E in `6a5a2f73`.
- Work through the Human-QA queue, prioritizing #2793, #2792, #2800, and #2766.

No unresolved product or architecture decision currently needs escalation.

## Blocked, parked, or anomalous

- `375dcc90`, `77353939`, `212a68ce` — Provider Usage completion is formally
  blocked on `b74833e7`. Preserve recovered commit `a5c386b` and all worktrees.
- `dd4f90b0`, `6a5a2f73` — operator-blocked; no signature weakening, mock
  E2E, or runtime workaround is allowed.
- `52892e8e` — coordinator plugin corrective implementation remains parked until
  host task `9e67c426` is accepted. Plugin PR #1 is not acceptance-ready.
- `21e3b8b8` and `b6fb09ba` — stale RUNNING sessions prevent the intended
  Review/QA work from starting. `c642d57a` owns the platform repair; old evidence
  will not be used to bypass the missing gates.
- ToDeploy tasks `b79288ed`, `20dd2959`, `61cdcf0a`, `575e304f`, `d059af5a`,
  and `6e0fc028` are intentionally retained until deployment/integration and
  terminal-cleanup criteria are explicitly verified.

## Most important next actions

1. Land, review, and QA `b74833e7`, then resume `212a68ce` to finish Provider
   Usage plugin coverage, package checksum, and the four real host E2Es.
2. Complete and independently test `c642d57a`, then recover the stranded
   `21e3b8b8` and `b6fb09ba` workflow gates without losing queued work.
3. Finish the remaining `957da1cb` workspace-mode/concurrency matrix and send it
   through Review and QA.
4. On the human's instance-update signal, immediately rerun `dd4f90b0` probes
   and `6a5a2f73`'s exact Docker userns/bwrap E2E.
5. Keep tested Human-QA heads immutable until the human records acceptance or a
   concrete defect.

## Additional coordinator workspace report — captured at 07:00

This section preserves the independently generated report already present on
`main` when the Kandev Daily workflow report was synchronized.

### Needs your decision

— none

### Awaiting your testing

- `9eaed209-a0f2-40a8-b60a-20ac6e2d6f14` — Draft-user cleanup is ready:
  review the dry run against the supplied TSV (5,020 users found; 4,694 members
  targeted), then verify the foreign-key and concurrent-cleanup paths before
  authorizing any real deletion; 51 tests and PHPStan are green.
- `6e637017-0efb-4d40-9ea7-32582910c9b1` — PR #2756 is open with green CI:
  run `make dev`, verify the app at `http://localhost:11854` and MCP at
  `http://localhost:11854/mcp`, then exercise list/upsert/delete and
  same-session wake delivery without creating a new task.

### Watch

- `7a5d334c-0d29-4407-a914-0c0e2eb136ce` — Frozen in Review with
  `REVIEW_PASSED`: unchanged commit `bda8fea` looped QA→Review twice; do not
  signal completion again until platform task
  `e1ea9424-b27a-45b9-a629-d3593a3c688f` resolves routing or an administrator
  selects the forward step.
- `e1ea9424-b27a-45b9-a629-d3593a3c688f` — New platform Spec for the
  unchanged-tree QA↔Review loop is RUNNING after exact-worktree `mise trust`
  recovery; next action is a state-transition trace and regression plan, then
  creator-owned Todo→Work handoff.
- `d89ac244-aa8b-4119-a2a1-10c95e31ad4c` — Intentionally frozen supporting
  diagnosis; resume only after durable task
  `6a5a2f73-87e1-4c08-a983-64f2456c3633` lands and a fresh exact bwrap probe
  shows residual work.
- `6a5a2f73-87e1-4c08-a983-64f2456c3633` /
  `dd4f90b0-0cbe-4cab-bdd9-6a3480487f89` — Durable userns fix and bridge-HTTP
  verification remain blocked on the human's Kandev instance update-complete
  signal; then run signed Debian probes/build, Alpine smoke, and the exact
  real-container userns E2E without weakening assertions.
- Wake delivery — Standup arrived correctly at 07:00 America/Montreal, but cycle
  delivery had a three-hour gap from 03:02 to 06:00; current heartbeat tasks
  remain an explicit temporary deviation and native flag tools remain
  unavailable.

### FYI

- `142d3987-8c6d-49a4-8cad-00ed17ddbb87` — Moved obsolete host-cron work to
  Done while preserving WIP `ffdea99`; documented the bounded terminal-cleanup
  rule and shared it on main in commit `f2692d8`.
- `7a5d334c-0d29-4407-a914-0c0e2eb136ce` — Unblocked one stale Review
  re-entry, then froze the task when the identical QA handoff looped again;
  preserved the valid passed review rather than misclassifying routing as a code
  failure.
- Coordinator policy — Kandev routines are the sole wake source; creator-owned
  Spec→Todo children are moved promptly Todo→Work; destructive deletion remains
  prohibited when a Done resolution preserves the audit trail.

### Board pulse

1 healthy, 0 stalled, 4 blocked/waiting, 0 awaiting a new human decision; deep
inspection stopped one routing loop, launched its platform Spec, and left
security-sensitive bwrap verification gated on the existing update-complete
signal.
