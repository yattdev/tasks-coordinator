# Daily standup — 2026-08-20

Generated at 07:03 America/Montreal after a complete board monitoring and action cycle.

## Board pulse

- 2 implementation tasks are active in Work, 1 task is in CI Fixup, and 1 Human-QA task is actively completing CI remediation.
- 18 tasks are in Human-QA. Seven retained LAN test instances are healthy and populated; several code-only tasks are ready for PR review without a runtime.
- 5 Coordinator-managed Todo tasks are intentionally waiting: 2 on the human's instance/network update signal and 3 on the Git-metadata repair.
- 2 tasks remain in ToDeploy, 17 are Done, and no unattended task is in Spec, Review, QA, or PR.
- Same-workspace relationship inspection remains degraded on the deployed backend until PR #2841 is merged and deployed.

## Completed or materially progressed since the previous standup

- Coordinator host/plugin architecture advanced substantially. Host PR #2793 is now at `b2e3df599`; retry-safe occurrence claims, profile reconciliation, base-prompt composition, fail-closed workflow policy, redacted prompt access, coordinator E2E coverage, and a strict-mode E2E selector regression were corrected. Canonical CI is still draining without a current failure.
- Coordinator plugin PR #1 reached Human-QA at immutable head `962431c` with terminal-green Build, CI, and GitGuardian checks plus independent Review/QA. It is reviewable now but remains under a release/merge hold until Kandev publishes the required host contract.
- Workspace reuse PR #2843 implemented canonical environment election, abandoned-claim recovery, durable Docker control credentials, attach-only Docker/SSH reuse, and SSH workspace identity. The exact warm-Docker failure is green; the latest SSH correction is under terminal CI watch at `b6d329117`.
- Durable workflow recovery (`c642d57a`) now includes completion-intent reconciliation, quiet-grace rearming, transition-keyed control prompts, durable delivery receipts and leases, retry/ack-gap recovery, verified spawn supervision, and authorized audited stale-turn settlement through `658b53308`.
- Git metadata enforcement (`b74833e7`) added narrow linked-worktree projections, native add/commit/fsck and ref-lock coverage, attachment refresh/rollback, standalone Codex policy rendering, and fail-closed SSH/Sprites/remote capability handling. Human-QA exposed one remaining durable multi-repo resume defect, so the task was correctly returned to Work.
- Parent workspace-source recovery PR #2842 gained typed provenance handling and an atomic reparent/authorization guard at `482359e9`; all review threads are resolved. Its remaining E2E failure is a timeout without a failing assertion, and the agent lacks permission to rerun it.
- Redmine docs PR #2706 is mergeable with 11 green checks and all 11 threads resolved. Companion PR #2724 has both threads resolved and remains conflict-gated for later integration.
- The sanitized immutable shared test seed was published and used only through task-owned writable clones. Required LAN instances were rebuilt from exact task heads without modifying the main container or its data.
- Durable Coordinator learning was consolidated into shared main at `80976d27`, including repository-qualified PR evidence, safe fast-forward limits, and post-action reconciliation.

## Coordinator actions and decisions

- Rejected `b74833e7`'s latest Human-QA readiness claim: multi-repository resume still derived paths from mutable repository/branch names instead of durable `TaskEnvironmentRepo.WorktreePath`. Moved Human-QA → Work with exact rename/layout, reconnect/restart, rollback, and security regressions; verified session `87d9dc76` RUNNING.
- Restarted `c642d57a` in bounded session `71f95705` to close observability metrics, PostgreSQL parity, and final restart/reserve-ack/cleanup acceptance accounting; verified RUNNING.
- Resumed `957da1cb`'s exact-head PR #2843 terminal CI watch. It must keep both warm-Docker and SSH reuse shards green before readiness.
- Preserved Provider Usage tasks `375dcc90`, `77353939`, and recovery `212a68ce` as blocked. Resume `212a68ce` first only after `b74833e7` passes a new independent Review and QA cycle.
- Preserved the Human-QA boundary: no merge, rebase, squash, rewrite, deployment, or acceptance transition was performed.
- Confirmed the earlier routine-targeting escalation was a false alarm: `a68df3ae` owns Kandev and `f2949187` independently owns Performcoop. Both workspaces' routines are valid; no operator should remove either set.

## Active work

- `b74833e7` — durable multi-repository worktree resume via persisted `WorktreePath`, with rename/branch-layout and reconnect/restart regressions.
- `c642d57a` — final delivery/completion observability, PostgreSQL parity, cleanup/retention, and acceptance-matrix closure.
- `9e67c426` — canonical PR #2793 and fork PR #12 exact-head terminal CI monitoring at `b2e3df599`; application checks are green so far, while fork-only preview/docs failures are qualified infrastructure.
- `957da1cb` — PR #2843 exact-head CI remediation/watch at `b6d329117`; warm Docker is proven green and the SSH canonical-path fix is awaiting terminal evidence.

## Ready for immediate human testing

All listed instances bind on `0.0.0.0`, are LAN-reachable, use task-owned writable clones of the sanitized shared seed, and retain credentials and exact test steps in their task plans.

- `7dac85e2` — per-MR GitLab automation: `http://192.168.50.131:18500`.
- `d30ca136` / `dabb2da9` — rich hover and rename-focus behavior: `http://192.168.50.131:18501`.
- `51781b28` — authenticated/private versus public plugin webhooks: `http://192.168.50.131:38531`.
- `e9c15a27` — mobile Add-folder behavior: `http://192.168.50.131:38532`.
- `21e3b8b8` — self-tagging MCP tools and live chip projection: `http://192.168.50.131:38533`.
- `ae8fc022` — tag display/sidebar integration: `http://192.168.50.131:38491`.
- `9ededcef` — executor-field hydration preservation: `http://192.168.50.131:41114`.

## Ready for PR review without a test instance

- `52892e8e` — coordinator plugin PR #1, exact head `962431c`, all checks green. Release/merge remains held until a Kandev release contains host contract `ff9b8b8ec`, followed by setting `min_kandev_version` to the first containing tag.
- `7ca86e53` — Redmine PR #2706; companion #2724 is review-clean but later integration-conflicted.
- `9ebf7cdc` — terminal-session reuse PR #2766.
- `725d47ae` — hermetic GitHub backend tests PR #2792.
- `55d2d589` — post-merge E2E runner fixes PR #2800.
- `9349b6e5` — same-workspace relation authorization PR #2841.

## Needs human action

- Signal when the Kandev instance/network/bwrap update is complete. That is the required gate for `dd4f90b0` signed APT/build probes and `6a5a2f73`'s exact userns+bwrap Docker E2E.
- A maintainer must approve or rerun the fork workflows for plugin-template PR #4 (`96e27238`); no task-side code failure is established.
- A maintainer with repository rights must rerun the timed-out E2E job on PR #2842 (`b6fb09ba`); the task agent's rerun was denied and the artifact contains no failing assertion.
- Reviewer re-review is required to clear stale top-level `CHANGES_REQUESTED` states on PR #2513 (`45f95870`) and host PR #2609 (`ae8fc022`). PR #2610 (`d30ca136` / `dabb2da9`) also needs genuine live screenshots and reviewer clearance.
- Human acceptance remains required for each Human-QA item; testing credentials and focused scenarios are stored in the corresponding task plans.

## Blocked, parked, degraded, or terminal

- `375dcc90`, `77353939`, and `212a68ce` are intentionally parked on `b74833e7`; preserve commit `a5c386b` and every worktree.
- `dd4f90b0` and `6a5a2f73` are operator-blocked. Do not weaken signatures/assertions or substitute mock verification.
- PRs #2724, #2676, #2608, #2696, #2705, and #2800 retain integration conflicts. Human-QA must not resolve them by rebasing or merging main.
- `52892e8e` is review-ready but release-blocked by the unreleased host contract.
- Same-workspace `list_related_tasks` remains unavailable on the deployed backend; PR #2841 is the tracked fix and no duplicate task was created.
- Done and ToDeploy tasks remain history/deployment-owned. No destructive cleanup was performed.

## Most important next actions

1. Finish, independently review, and QA `b74833e7`; then resume `212a68ce` and complete Provider Usage concurrency/schema/shared-cache acceptance.
2. Drive PR #2843's warm-Docker and SSH suites to a terminal clean run, then advance only with exact-head Review/QA evidence.
3. Close `c642d57a`'s metrics, PostgreSQL, restart, and cleanup matrix and send it through independent Review and QA.
4. Finish canonical PR #2793 CI, resolve every remaining branch-owned review thread, and preserve the coordinator plugin's host-release hold.
5. Use the seven LAN instances for focused Human-QA and record acceptance or concrete defects against their immutable heads.
