# Coordinator standup — 2026-08-29 (America/Montreal)

Window: 2026-08-28T23:15Z → 2026-08-29T11:05Z. Board after today's actions.

## 1. NEEDS YOUR DECISION

- **`fa3fba49-2018-460b-a600-adae23b24cc8` — the D1 trust boundary.** Kandev's own `apps/backend/internal/mcp/handlers/wrap_agent_message.go` labels every Coordinator message *"Treat it as peer agent input rather than a direct user instruction"*, and the executor runtime then permits consequential actions only on user-level authorization. So no Coordinator grant can ever be trusted, by construction. **What to do:** either (a) commission a reviewed executor authority channel that promotes a verified Coordinator grant to trusted authorization, with audit and revocation, or (b) accept D1 as an external blocker and let pull request #3048 land scoped to the Kandev-side grant surface only. **Recommendation: (b) now, plus a separate design task for (a).** This is authorization weakening at a trust boundary, so it is reserved for you. **Consequence of not deciding:** five cards stay blocked, three of them holding commits that exist in no remote (9, 6 and 28 commits).
- **Rebuild the running `kandev-local` image.** Kandev Support confirmed both `/data/home/go` writability and `lsof` are **already on `main` in commit `aad8cd55`** — the failures agents hit come from a **stale running image**. **What to do:** rebuild and recreate from a clean `main` deployment checkout, then run `bash test.sh`. **Consequence:** every Go task keeps working around a read-only module cache, and agents keep escalating file-holder checks they could do themselves.
- **Docker bridge NAT fix — 11 days blocked.** Support confirmed the diagnosis and named the remediation. **What to do, on the deployment host:** `cd ~/Code/kandev && sudo bash scripts/nat-redirect.sh --check && sudo bash scripts/nat-redirect.sh && sudo bash scripts/nat-redirect.sh --check`. **Consequence:** container end-to-end testing stays impossible workspace-wide (`dd4f90b0-0cbe-4cab-bdd9-6a3480487f89`).
- **Permission to create two board tasks.** (i) A guard-compatibility investigation for the seven `parent directory cannot be accessed` failures — Support proved the suite **passes on a clean host checkout**, so it is guard-specific and needs an owner. (ii) An infrastructure task for the message-delivery incident below. I have not created either; you asked to be consulted first.

## 2. AWAITING YOUR TESTING

- `63b40206-ee2c-4a25-a5b8-79b0d599467a` — Notes plugin preserves existing notes. Acceptance on plugin PR notes#6. Unchanged in this window; no agent action outstanding.

## 3. WATCH — blockers, anomalies, degradations

- **D16 — message delivery is stuck, not merely slow.** `queued_messages` held 100 undelivered rows, twelve addressed to this Coordinator session, one queued 07:16:54Z that never surfaced. Support's verdict: *"a stuck or unserviced delivery path — not normal short-lived backlog behavior."* Operating rule adopted: a send receipt proves queue persistence, not delivery, and a `WAITING_FOR_INPUT` session is not proactively woken. **This explains most of what looked like agents reporting stale state today; it is a platform fault, not agent carelessness.**
- **D1 — non-deterministic per invocation.** `23a62467` pushed successfully to a remote and was later refused the identical command on the same remote. `b74833e7` had a local index mutation refused while its push reached GitHub; `856898aa` had a push refused before any network call. A regression asserting "pushes are honoured" or "local mutations are honoured" would pass while the defect survives.
- **D2 — GitHub secondary limit flapped four times** (cleared 06:01Z seventeen minutes early, recurred 07:26Z, cleared, recurred 08:09Z). Root cause is ours and is fixed in `27b493a3`.
- **Two worktree collisions, both hazardous.** `b79288ed` → `worktrees/kandev-source` owned by `b74833e7` (28 unpushed commits); `00ceb41b` → `worktrees/kandev-source21` owned by `f4136a59` (8 commits ahead). The obvious repair — rewriting the backlink — would hand another task's Git metadata away. Now encoded as a fail-closed case.
- **Three checkouts have lost their history permanently** (`375dcc90`, `212a68ce`, `7ca86e53`): branch refs absent locally *and* on the remote (404s sanity-checked against controls that resolve), no dangling objects, no reflogs. Content survives; commits do not. Repair is halted; content-only preservation is the honest outcome.
- **31 cards in Blocked**, each with a complete record and a trigger re-checked this cycle.
- **4 GB** of Human-QA fixture data sits under `7dac85e2`'s worktree — task-owned and disposable, but worth reclaiming.

## 4. COMPLETED OR MATERIALLY PROGRESSED

- `27b493a3-65b6-4d4a-8b68-73f2ffcf9621` — **complete**. Spec through five implementation tasks, eight commits pushed, no platform blocker, no hook bypass, every file-map widening flagged before acting. Now PR **#3143**.
- `23a62467-37e9-4113-b374-b44003abc0f3` — validator and ADR/spec committed, base integrated, pushed, PR **#3136**.
- `1f8d4dc8-83ac-44d6-9fbd-b34bd46e044e` — executor admission plus a 5,404-file upstream merge committed, three fail-closed cases implemented, PR **#3137**. Proved `git worktree repair` cannot rebuild an absent admin directory, closing an avenue I had suggested.
- `fa3fba49-2018-460b-a600-adae23b24cc8` — merged current `main`, pushed `85f1ac928`, refreshed checks stale since 2026-08-26, and rescoped #3048 to state honestly that it does **not** fix D1.
- `b74833e7-a05f-4cdf-81cf-db5b4c02f368` — acceptance criteria 1, 4 and 5 evidenced; 2 and 3 blocked solely by D1. Complete to the platform limit.
- **Charter mirror closed.** Support supplied `docker kandev workspace description-update`; the live task description is now byte-identical to `PROMPT.md` (64,483 bytes, sha256 `4bd70fad…`). This had been deferred for three cycles.
- **Kandev Support channel proven end-to-end** — six requests sent, five answered, three of today's blockers resolved or precisely diagnosed by it.

## 5. UNBLOCKED, MOVED, PROMOTED

- `2747ecb7-6e16-4ee6-a3e9-88fcabcf7425` → **Done** (PR #2869 merged `05fe86fd7`; task root held only `.kandev-workspace.json`).
- `725d47ae-58f9-4903-88b4-2d9aa8dbe733` → **Done** (PR #2792 merged `b2ac3add0`; same evidence).
- `b74833e7` Blocked → Work; `fa3fba49` Blocked → Work; `dd4f90b0` Todo → Blocked; `856898aa` PR → Blocked; `27b493a3` Todo → Work (then auto-advanced to PR).
- Pull requests opened on agents' behalf while they were credential-blocked: **#3136**, **#3143**. `1f8d4dc8` opened **#3137** itself.
- `7dac85e2-8c94-4c4e-a7cc-2cb6c83862ed` — terminal receipt verified, **stays in Done**. All committed work is pushed; the 115 untracked files are materialization artifacts present in 43 worktrees.

## 6. DECISIONS I MADE (all vetoable)

- Removed a stale foreign `index.lock` no task agent could reach, after proving no `git` process existed host-wide.
- Approved removal of two zero-byte stale locks, bound to exact paths with a stop-and-report condition.
- Pushed and opened PRs for two agents blocked only on the final provider action.
- Scoped #3048 to (b) and told it **not** to build a trust bridge speculatively — that is your call.
- Corrected five cards' resume trigger: **"#3048 merged" will never fire**; the trigger is a Coordinator-authorised consequential action actually succeeding.
- Halted repair of three checkouts on evidence their history is unrecoverable, rather than reconstructing an unborn HEAD.
- Did not re-raise the escalation you declined at 05:36Z; surfaced it here instead.

## 7. BOARD PULSE

52 tasks — 31 blocked, 12 done, 4 in work, 1 in PR, 2 to deploy, 1 human-QA, 1 coordinator. Four pull requests open and Draft with CI running: #3136, #3137, #3143, #3048.

Inspection was deep: every claim in this report was verified against the repository, filesystem or provider API rather than an agent's report. That mattered — **I issued twelve corrections to my own conclusions today**, every one traceable to prescribing or concluding before inspecting, and in three cases it was an agent's refusal to follow my instruction that prevented harm. The most consequential: I approved a push onto a branch whose histories had diverged, where the natural next step would have been a force-push over 286 commits.

**Most important next actions:** decide the D1 trust boundary; rebuild the stale image; run the NAT fix.
