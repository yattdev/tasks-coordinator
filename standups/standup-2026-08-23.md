# Coordinator standup — 2026-08-23 (07:00 America/Montreal)

## 1. NEEDS YOUR DECISION
- **375dcc90 (Provider Usage MCP) — session fails to start; fix awaiting your go.** Root cause: stale-worktree collision — its plugin worktree dir/branch already exist, so env prep's `git worktree add` aborts ("… already exists"). Recovered commit a5c386b is COMMITTED and held by branch feature/expose-provider-usag-nn9 (verified clean), so a `git worktree remove --force` + prune is safe/reversible and loses no work. Recommendation: authorize it; I clear the collision and a session can start. (Its actual impl work is still gated behind b74833e7 landing.)
- **Human-QA → ToDeploy scope for CI-green tasks.** You asked CI-green tasks to proceed. Which to advance: (a) review-only CI-green ones — 725d47ae, 5d27f4a2, 55d2d589, 957da1cb, dacee5a8, b74833e7 (my recommendation, they never needed hands-on testing); (b) all CI-green including instance-backed; or (c) none — you accept them after testing. Instance-backed features have live LAN instances built specifically for your testing; moving those to ToDeploy skips it.

## 2. AWAITING YOUR TESTING (Human-QA — 21 tasks, 16/16 LAN instances up at 192.168.50.131)
- Newly ready after the base repair + your rebase directive: c642d57a #2909 (39 checks green; 1 known E2E flake), b74833e7 #2845 (rebased, 22 threads resolved), dacee5a8 #2910 (rebased, mergeable, review-ready), 9349b6e5 #2841 (rebased, mergeable), 51781b28 #2608 (rebased), 6a5a2f73 #2937 (userns E2E now runnable after the NAT fix).
- Instance-backed, ready to click through: 9e67c426, 153cdbbe, 52892e8e (view/load only), 7ca86e53+00ceb41b (Redmine), 51c2875b, f4136a59, 45f95870, 9ebf7cdc. Logins verified earlier by direct login test; registry in prior cycle notes.
- Review-only (no instance): 725d47ae, 5d27f4a2, 957da1cb, dacee5a8, 55d2d589.
- dfb3f5c7 (tags filter): plugin PR #8 MERGED; host PR still open, subtask 54ed03f7 finishing CI.

## 3. WATCH
- **GitHub API degraded (intermittent, account-wide).** Secondary rate-limit + per-session credential-lease 401s (`git_credential_lease_invalid`) intermittently block CI reads and PR-comment read/reply — NOT code work (rebases/tests/commits proceed). Workaround in use: I relay blocked review comments via my own working credentials (did this for dacee5a8). Surface only if it turns persistent.
- **Live DB integrity defect — unowned.** Six independent tasks reported task_walkthroughs anomalies (NUMERIC task_id, NULL timestamps, missing index rows) in /data/data/kandev.db, present in source + byte-verified copies. Awaiting your file-or-ignore call.
- GitHub Actions runner queue saturated after the wave of rebases — several PRs show "checks queued" (not failing). Clears as runners free.
- c0db9627 (Review) "Recover stale task worktree path" — new recovery task, actively in Review; watching it does not loop.

## 4. FYI (decisions I made on your behalf — vetoable)
- Held routine task DISPATCH while upstream/main did not compile; LIFTED when the base compiled again (repaired by #2916, verified by build). Confirmed with a bridge-container `apt-get update` that your host NAT fix works.
- dd4f90b0 (Docker/APT NAT defect) → Done: verified zero-diff host-config fix (no code PR); its userns E2E criterion reassigned to 6a5a2f73, which then advanced with #2937.
- Active-management sweep (per your direction): moved 55d2d589 #2800 and c642d57a #2909 CI-Fixup→Human-QA (green); moved 6a5a2f73 Spec→Work to run the userns gate; nudged the stalled tags feature (dfb3f5c7) — both PRs opened, plugin #8 merged.
- Relayed your rebase-onto-upstream/main directive to the 4 genuinely-conflicting PRs (b74833e7, dacee5a8, 51781b28, 9349b6e5); all rebased, conflicts resolved on semantics, threads addressed. Did NOT touch the many "blocked-on-checks" (non-conflicting) PRs.
- Operational unblocks (blessed, vetoable): `mise trust` on dfb3f5c7's host subtask worktree (was failing at ACP init); set a disposable Redmine admin password on the throwaway :13080 instance at your request.
- Ran learning cycles; merged to shared main (zero-diff Done policy, base-breakage dispatch hold, integration-pending, mise-trust/Backlogs startup recoveries, boundary-verify-on-resume).

## 5. BOARD PULSE
21 Human-QA (awaiting your testing/acceptance) · pipeline drained to 13 ToDeploy · active: c0db9627 (Review), 54ed03f7 (#2932 CI Fixup, finishing) · parked: Provider-Usage cluster 375dcc90 (FAILED, needs the worktree fix)/212a68ce, both dependency-gated on b74833e7. Done shows 4 (72h auto-archive cleared older entries — archival, not loss). Most important next actions are BOTH yours: authorize the 375dcc90 worktree fix, and pick the Human-QA→ToDeploy scope. Everything agent-side is converged or self-finishing.
