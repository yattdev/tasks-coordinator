# Compact charter extraction verification

The Human's cost-optimization request targets this board Coordinator first.
The mandatory charter is now a compact kernel plus an action-to-section index.
The full operating policy remains jointly binding and full monitoring cycles
still load it, including complete Done coverage. Unknown/missing/contradictory
routes require the whole policy before action. Narrow turns retrieve complete
applicable sections; all action gates remain in force.

Verification before publication:

- Original charter preserved exactly as `docs/OPERATING_POLICY.md`: 144579 bytes,
  SHA256 `2266e2b75bfd9388b8e9003e3e495f69572dfa34c68e6aeedebfe7f0235d2cfd`;
  comparison with committed pre-extraction PROMPT returned byte equality.
- New mandatory PROMPT: 14121 bytes, 90.2% fewer bytes. This is a file-size
  comparison, not measured token/credit savings or a managed-history reset.
- Independent read-only Sol audit verified every route prefix exists uniquely,
  authority/lane/queue/provider/gate/continuity floors remain, and loaders/docs
  agree. Its one finding about historical contract provenance was corrected.
- Existing 109 contract/cycle tests and 32 RFC tests pass; `git diff --check`
  passes. Contract JSON/digest is unchanged; historical 1.2.0 provenance does not
  prove the plugin enforces newer routing policy.
- Preserved policy links retain repository-root-relative interpretation as
  specified in the kernel; browser rendering of historical relative links may
  differ. No policy text was rewritten merely to repair archival links.

The old policy and Git preimage permit rollback. The change does not switch
models, modify Kandev Automation, enable live dispatch or waive real-board
validation. Track actual loaded bytes and outcome parity during rollout.
