# Claude project instructions

Before every turn, read `AGENTS.md` and then read `PROMPT.md` completely. The
latter is the canonical Coordinator kernel and policy retrieval index. Load its
required complete sections of `docs/OPERATING_POLICY.md` before action; use the
whole-policy fallback when uncertain. Also read the live Kandev
Coordinator task plan before any board action. Do not treat conversation memory
or this compatibility file as a substitute. Before yielding or hitting a known
model limit, run the continuity checkpoint in `PROMPT.md` and
`docs/CONTINUITY.md`.
